temp = input('Please enter the temprature value: \n');
unit = input('Please enter the temprature unit (C / F): ', 's');
if unit == 'C'
    % Convert from C to F
elseif unit == 'F'
    % Convert from F to C
else
    